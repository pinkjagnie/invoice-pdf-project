import React from "react";

import styles from "./about.module.css";

const About = () => {
  return(
    <div className={styles.aboutFillingOutForm}>
      <p>Complete the form fields below. Make sure the data you entered is correct.</p>
    </div>
  )
};

export default About;